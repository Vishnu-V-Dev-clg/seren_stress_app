name: seren_stress_app
description: A real-time GSR stress analysis app.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  fl_chart: ^0.66.0
  web_socket_channel: ^2.4.0
  google_fonts: ^6.1.0
  cupertino_icons: ^1.0.2
  firebase_core: ^2.24.0
  cloud_firestore: ^4.17.5
  uuid: ^4.5.2
  http: ^1.6.0
  firebase_database: ^10.5.7
  socket_io_client: ^3.0.2
  path_provider: ^2.1.5
  share_plus: ^10.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
